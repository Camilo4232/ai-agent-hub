{
  "name": "agent-registry-contracts",
  "version": "1.0.0",
  "description": "ERC-8004 Smart Contracts for AI Agent Registry with X402 Payment Integration",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-all.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy-all.js --network localhost",
    "test": "hardhat test",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "verify": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0",
    "chai": "^4.3.10",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  }
}
